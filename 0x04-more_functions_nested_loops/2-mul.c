#include "main.h"

/**
 * mul - multiplies the input
 * @a: parameter being multiplied
 * @b: parameter being mulltiplied
 *
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;

	return (prod);
}
