#include "main.h"

/**
 * _abs - prints absolute values of nums
 * @n:  number being converted
 * Return: the abs of the integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
